

void connect_to_server(int32_t ip, uint16_t server_port, uint16_t local_port)
{
    // primeste ip-ul si portul serverului si portul pe care clientul doreste sa primeasca pachetele
    // trimite un pachet serverului cu tipul de comunicatie si portul pe care clientul
    // vrea sa primeasca pachete
    // se creaza canalul de comunicatie cu serverul
}

void send_message(char *buf, int32_t len)
{
    // trimite un mesaj
}

void close_connection()
{
    // inchide conexiunea
}